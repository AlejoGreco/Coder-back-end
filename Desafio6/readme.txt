Realizar un servidor express con 2 end points

El primero sera /productos y respondera con un arreglo de productos leidos desde un archivo de texto o harcodeados en el codigo.

El segundo sera /productoRandom y respondera con un producto al azar leido desde las mismas fuentes previas

Para sekeccionar de donde se obtendra la info se usa la variable: WITH_FS