export const config = {
    host: 'localhost', 
    port: 8080
}
  
export  const auth = {
    path: 'user/login',
    credentials: {
      "username": "trici1@gmail.com",
      "password": "12345"
    }
}