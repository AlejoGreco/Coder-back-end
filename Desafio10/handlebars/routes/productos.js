const express = require('express')
const route = express.Router()

const productos = []

route.get('/', (req, res) => {

})

route.post('/', (req, res) => {
    
})

module.exports = {route, productos}